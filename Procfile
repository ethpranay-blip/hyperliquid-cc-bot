# Procfile for Railway deployment
# Railway runs this as the main process and handles auto-restart, graceful shutdown

# Use python3 directly (Railway provides process supervision)
# The bot runs a NiceGUI web dashboard on port 8080
web: python3 -m app.main
